name: CI

"on":
  push:
    branches: [blackboxai/init-repo]
  pull_request:
    branches: [blackboxai/init-repo]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate README exists
        run: test -f README.md
